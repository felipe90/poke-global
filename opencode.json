{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "figma": {
      "type": "local",
      "command": [
        "bash",
        "-lc",
        "set -a && source .env && set +a && exec npx -y figma-developer-mcp"
      ],
      "cwd": ".",
      "enabled": true
    }
  }
}
